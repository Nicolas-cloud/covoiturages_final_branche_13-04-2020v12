<!DOCTYPE html>
<html>
    <title>{% block title %}Besoin d'un covoiturage ?{% endblock %}</title>

    <head>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        
        <link rel="stylesheet" type="text/css" href="{{ asset('css/bootstrap.min.css') }}">
        
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
        
        <script type="text/javascript" src="https://code.jquery.com/jquery-1.9.0.js"></script>
        
        <script src='https://kit.fontawesome.com/a076d05399.js'></script>

    </head>


    {% block javascript %}
        
        <script type="text/javascript" src="{{ asset('js/bootstrap.min.js') }}"></script>
        <script type="text/javascript">

        $(document).ready(function(){           
            $('#fixed-form-container .body').hide();
            $('#fixed-form-container .button').click(function () {
                    $(this).next('#fixed-form-container div').slideToggle(400);
                    $(this).toggleClass("expanded");
            });
        });
        </script>
        
    {% endblock %}


<body>
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <uL>
                <div class="marge">
                    <li><a href="{{ path('home') }}" class="lien_left"><i class="fa fa-home" style='font-size:20px; margin-right:5px;'></i>{% trans %}Home{% endtrans %}</a></li>
                    <li><a href="{{ path('trajet.list') }}" class="lien_left"><i class="fa fa-car" style='font-size:15px; margin-right:5px;'></i>{% trans %}Available routes{% endtrans %}</a></li>
                    <li><a href="{{ path('search.trajet') }}" class="lien_left"><i class="fa fa-search" style='font-size:15px; margin-right:5px;'></i>{% trans %}Search{% endtrans %}</a></li> 
                    <li><a href="{{ path('trajet.create') }}" class="lien_left"><i class="fa fa-plus" style='font-size:15px; margin-right:5px;'></i>{% trans %}Suggest a route{% endtrans %}</a></li>
                    <li><a href="#" class="lien_left"><i class='far fa-comment' style='font-size:15px; margin-right:5px;'></i>{% trans %}Contact us{% endtrans %}</a></li>
                    {% if is_granted('ROLE_USER') %}
                        <li><a href="#" class="lien_left"><i class="fa fa-user" style='font-size:15px; margin-right:5px;'></i>{% trans %}Private space{% endtrans %}</a></li>
                    {% endif %}
                    {% if is_granted('ROLE_USER') %}
                        <li><a href="{{ path('app_logout') }}" class="lien_right">{% trans %}Sign out{% endtrans %}</a></li>
                    {% else %}
                        <li><a href="{{ path('membre_create') }}" class="lien_right">{% trans %}Registration{% endtrans %}</a></li>
                    {% endif %}
                    {% if is_granted('ROLE_USER') %}
                    {% else %}
                        <li><a href="{{ path('app_login') }}" class="lien_right">{% trans %}Sign in{% endtrans %}</a></li>
                    {% endif %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <li><a href="{{ path('admin_administration') }}" class="lien_right">{% trans %}Administration{% endtrans %}</a></li>
                    {% endif %}
                    {% for locale in locales if locale != app.request.locale %}
                        <li class="nav item activ">
                            <a class="lien_right" href="{{ path('change_locale', {'locale' : locale}) }}"><img src="{{ asset('img/'~locale~'.png') }}" alt="{{locale}}"></a>
                        </li>
                    {% endfor %}
                </div>
            </ul>
        </div>
    </nav>

    <section id="fixed-form-container">
        <div class="button">Click Me!</div>
            <div class="body">
            <div class="form-group">
            <label for="email">Email address:</label>
            <input type="email" class="form-control" id="email">
        </div>
        <div class="form-group">
            <label for="pwd">Password:</label>
            <input type="password" class="form-control" id="pwd">
        </div>
        <div class="checkbox">
            <label><input type="checkbox"> Remember me</label>
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
        </div>
    </section>
    
    <div class="container">

        {% block body %}

        {% endblock %}
    </div>



</body>
</html>